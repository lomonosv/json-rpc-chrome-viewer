import React from 'react';
import ReactDOM from 'react-dom/client';
import Application from '~/components/Application';
import './index.scss';

ReactDOM.createRoot(document.getElementById('application')).render(<Application />);
