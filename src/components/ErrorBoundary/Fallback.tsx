import React from 'react';
import styles from './fallback.scss';

const Fallback = () => (
  <div className={ styles.container }>
    Something went wrong. Please reopen the plugin.
  </div>
);

export default Fallback;
